&НаСервере
Функция УдалитьУслугу(СписокУслуг)
	ЗатронутыеДокументы = Новый Массив;
	
	Для Каждого СсылкаУслуги ИЗ СписокУслуг Цикл
		ЗатронутыеДокументы.Добавить(УправлениеБТСервер.УдалитьУслугу(СсылкаУслуги, Истина));	
	КонецЦикла;
	
	Возврат ЗатронутыеДокументы; 
КонецФункции


&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ЗатронутыеДокументы = УдалитьУслугу(ПараметрКоманды);
	
КонецПроцедуры
