

Процедура ВставитьСообщение(Объект, Сообщение, ТипОписания = 0, ДанныеСобытия = Неопределено) Экспорт
	
	МЗ = РегистрыСведений.ИсторияОбъектов.СоздатьМенеджерЗаписи();
	МЗ.ДатаВремя = ТекущаяДата();
	МЗ.Объект = Объект.Ссылка;
	МЗ.ОписаниеСобытия = Сообщение;
	МЗ.ТипОписания = ТипОписания; 
	МЗ.ДанныеСобытия = ДанныеСобытия;
	МЗ.Пользователь = ПараметрыСеанса.ТекущийПользователь;
	МЗ.Записать();
	     
КонецПроцедуры